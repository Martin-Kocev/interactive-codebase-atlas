interface:
  display_name: "Orchestrator"
  short_description: "Coordinate subagents and verify their work"
  default_prompt: "Use $orchestrator to coordinate this task across specialized subagents and independently verify the integrated result."
